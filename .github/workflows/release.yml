name: Release
on:
  push:
    tags:
      - "v*"
concurrency:
  group: release-homebrew-tap
jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: rubykatzen/baseline/.github/actions/create-release@v0.5.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
