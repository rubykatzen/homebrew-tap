name: Telegram Release Notify
on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch:
jobs:
  notify:
    uses: rubykatzen/releaser/.github/workflows/notify-telegram-unreleased-shared.yml@v0.4
    secrets: inherit
