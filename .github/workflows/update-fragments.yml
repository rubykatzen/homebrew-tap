name: Update embedded fragments
on:
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  update:
    uses: rubykatzen/embedder/.github/workflows/update-fragments-shared.yml@v0.1
