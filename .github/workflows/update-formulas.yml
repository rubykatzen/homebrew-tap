name: Update Formulas
"on":
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: update-homebrew-formulas
  cancel-in-progress: false
jobs:
  update:
    uses: rubykatzen/releaser/.github/workflows/update-homebrew-formulas-shared.yml@fix/homebrew-formula-path
