name: Dependabot Automerge
on:
  pull_request_target:
    types: [opened, reopened, synchronize]
jobs:
  merge:
    uses: rubykatzen/releaser/.github/workflows/merge-dependabot-pr-shared.yml@v0.5
    secrets: inherit
