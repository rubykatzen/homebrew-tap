name: Update embedded fragments
on:
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  update:
    uses: rubykatzen/embedder/.github/workflows/update-fragments-shared.yml@v0.0.6
    with:
      embedder-ref: v0.0.6
